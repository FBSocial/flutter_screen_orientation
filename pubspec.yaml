name: flutter_screen_orientation
description: "实时监测Android/IOS屏幕方向的插件"
version: 1.0.1+1
homepage: https://github.com/515445681/flutter_screen_orientation

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' and Android 'package' identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: cn.xiaoguikeji.flutter.screen.flutter_screen_orientation
        pluginClass: FlutterScreenOrientationPlugin
      ios:
        pluginClass: FlutterScreenOrientationPlugin
